<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.cktext_area :body, ckeditor: { allowedContent: true } %>
  </div>
  <div class="field">
    <%= f.label :header_content %><br />
    <%= f.cktext_area :header_content, ckeditor: { allowedContent: true } %>
  </div>
  <div class="field">
    <%= f.label :right_content %><br />
    <%= f.cktext_area :right_content, ckeditor: { allowedContent: true } %>
  </div>
  <div class="field">
    <%= f.label :meta_title %><br />
    <%= f.text_field :meta_title %>
  </div>
  <div class="field">
    <%= f.label :meta_description %><br />
    <%= f.text_field :meta_description %>
  </div>
  <div class="field">
    <%= f.label :keywords %><br />
    <%= f.text_field :keywords %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
