<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:meta_title)) %></title>
    <meta name="viewport" content="initial-scale=0.3">
    <meta name="description" content="<%= yield(:meta_description) %>" />
    <meta name="keywords" content="<%= yield(:meta_keywords) %>" />
    <link rel="icon" href="http://www.hydroflask.com/media/favicon/default/favicon.jpeg" type="image/x-icon" />
    <link rel="shortcut icon" href="http://www.hydroflask.com/media/favicon/default/favicon.jpeg" type="image/x-icon" />
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
		<%# Can't do it this way -- asset pipeline blows up on mising .js files %>
    <%#= javascript_include_tag "application", controller_name %>
    <%= csrf_meta_tags %>
    <!--[if lt IE 9]>
    <script src="http://ie7-js.googlecode.com/svn/version/2.1(beta4)/IE9.js"></script>
    <![endif]-->
  </head>
  <body>
      <%= render 'layouts/header' %>
			<% if current_page? root_url %>			
        <%= yield %>
      <% else %>
        <div id="body_wrapper" class="wrapper">
          <div id="contentWrapper">
						<%= render partial: "shared/show_flash_messages", flash: flash %>
            <%= yield %>
          </div>
        </div>
      <% end %>
      <%= render 'layouts/footer' %>
      <% if Rails.env.production? %>
        <script type="text/javascript">
        //<![CDATA[
          (function() {
            var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
            ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
            (document.getElementsByTagName('head')[0] || document.getElementsByTagName('body')[0]).appendChild(ga);
          })();

          var _gaq = _gaq || [];

          _gaq.push(['_setAccount', 'UA-12454141-1']);
          _gaq.push(['_trackPageview']);

        //]]>
        </script>
      <% end %>
  </body>
</html>
