<% provide(:meta_title, @brand.title) %>
<% provide(:meta_description, @brand.meta_description) %>
<% provide(:meta_keywords, @brand.keywords) %>
<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @brand.name %>
</p>


<%= link_to 'Edit', edit_brand_path(@brand) %> |
<%= link_to 'Back', brands_path %>
