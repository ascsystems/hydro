<% if lid_counter == 0 
		selected = 'selected'
	else
		selected = ''
	end
%>
<%= image_tag "options/#{lid.display_data}", alt: lid.name, title: lid.name, class: "lid option #{selected}", option_id: lid.id %>